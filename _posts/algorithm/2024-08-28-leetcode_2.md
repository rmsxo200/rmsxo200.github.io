작업중

String s = "LVIII";
int sum = 0;
char[] romanArr = {'I','V','X','L','C','D','M'};
Map<Character, Integer> romanMap = new HashMap<Character, Integer>(){
    {
        put('I', 1);
        put('V', 5);
        put('X', 10);
        put('L', 50);
        put('C', 100);
        put('D', 500);
        put('M', 1000);
    }
};

for (int i = 0; i < s.length(); i++) {
    int prevIdx = Arrays.asList(romanArr).indexOf(s.charAt(i));
    int nextIdx = Arrays.asList(romanArr).indexOf(s.charAt(i+1));
    if(prevIdx < nextIdx) {
        i++; //1증가
        
    } else {
    
    }
}
